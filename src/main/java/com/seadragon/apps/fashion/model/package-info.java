@FilterDefs({
	@FilterDef(name="filterByDate")
})

package com.seadragon.apps.fashion.model;

import org.hibernate.annotations.FilterDef;
import org.hibernate.annotations.FilterDefs;
import org.hibernate.annotations.ParamDef;

